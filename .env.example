# GA4 measurement ID. Analytics is cookieless (no consent banner) — see
# src/lib/analytics.ts. Leave this unset and nothing is sent at all, which is
# what you want for local development.
PUBLIC_GA_MEASUREMENT_ID=
