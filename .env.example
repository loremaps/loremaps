# GA4 measurement ID. Production builds show an opt-in consent banner and do
# not load Google Analytics until the visitor accepts. Leave this unset to
# disable the banner and analytics entirely.
PUBLIC_GA_MEASUREMENT_ID=
